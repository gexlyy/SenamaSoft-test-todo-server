### To launch the app you need to create a db in mongodb, named "todobd", then start mongodb server locally.

### To start the web server type `npm start`,

### Server runs in http://localhost:5000   

